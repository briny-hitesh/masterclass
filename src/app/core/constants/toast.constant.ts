export const TOAST_CONFIG: {
    keyboardClose: boolean;
    duration: number;
    position: "top" | "bottom" | "middle" | undefined;
} = {
    keyboardClose: true,
    duration: 1000,
    position: "top",
}